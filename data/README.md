# Local data (not synced to GitHub)

Runtime files live here on the collector machine:

- `collector-state.json` — which tweets were already scanned
- `collector-schedule.log` — scheduled run log
- `failed/` — tweets that failed to import

These paths are listed in `.gitignore`.
