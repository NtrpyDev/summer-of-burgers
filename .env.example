# https://docs.x.com/x-api/getting-started/getting-access
# For reading public posts you only need Bearer Token (app-only auth).

X_BEARER_TOKEN=

# Optional: only if you run scripts/refresh-bearer.cmd to mint a new bearer
X_API_KEY=
X_API_SECRET=
